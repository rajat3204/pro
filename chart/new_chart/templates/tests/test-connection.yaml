apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "new_chart.fullname" . }}-test-connection"
  labels:
    {{- include "new_chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "new_chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
